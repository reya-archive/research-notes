# 개요

> 작성일: YYYY-MM-DD
> 상태: 초안

리서치의 배경과 목적을 적습니다. 이 파일은 Markdown 원본이며,
화면용 HTML은 같은 폴더의 `index.html` 또는 `pages/*.html`로 별도 작성합니다.

## 메모

- 리서치 문서가 여러 개라면 `02-method.md`, `03-findings.md` 식으로 추가하세요.
- 파일명 prefix(`01-`, `02-`)는 정렬을 위해 두 자리 숫자를 사용합니다.
